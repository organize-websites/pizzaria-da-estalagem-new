name: pizzariadaestalagem
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.1.0+3

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.2
  webview_flutter: ^2.0.4
  url_launcher: ^6.0.3
  splashscreen: ^1.3.5
  font_awesome_flutter: ^9.0.0
  http: ^0.13.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - imagens/ic_launcher.png
    - imagens/logo_p.png
    - imagens/logo-app.png
    - imagens/logo-gup.png
    - imagens/splashscreen.png
